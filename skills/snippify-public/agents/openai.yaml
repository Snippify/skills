interface:
  display_name: "Snippify Public"
  short_description: "Browse approved public Snippify knowledge"
  default_prompt: "Use $snippify-public to find relevant public knowledge in Snippify."
dependencies:
  tools:
    - type: "mcp"
      value: "snippify"
      description: "Anonymous Snippify public Artifact MCP server"
      transport: "streamable_http"
