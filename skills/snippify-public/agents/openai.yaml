interface:
  display_name: "Snippify Public"
  short_description: "Discover Artifacts, then fetch selected content"
  default_prompt: "Use $snippify-public to discover relevant public or owned private Artifacts and retrieve only the content needed."
dependencies:
  tools:
    - type: "mcp"
      value: "snippify"
      description: "Snippify read-only Artifact MCP server with optional bearer authentication"
      transport: "streamable_http"
